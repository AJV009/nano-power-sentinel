# See resilience-restart.conf. This variant only removes the start-rate limit
# and leaves the unit's own Restart= policy alone, for units that already have
# a deliberate one.
[Unit]
StartLimitIntervalSec=0
